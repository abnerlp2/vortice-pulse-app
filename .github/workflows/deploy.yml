name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Disparar redeploy en EasyPanel
        run: curl -fsS -X POST "$WEBHOOK"
        env:
          WEBHOOK: ${{ secrets.EASYPANEL_WEBHOOK_PULSE }}
